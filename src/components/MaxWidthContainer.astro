---
const { width = "740px" } = Astro.props;
---

<div class="container">
  <slot />
</div>

<style define:vars={{ width }}>
  .container {
    max-width: var(--width);
    margin: 0 auto;
  }
</style>
